import React, { useState } from "react";
import { Alert, Image, StyleSheet, Text, Pressable, View } from "react-native";

const CustomImage = ({width, height, source}) => {
  return (
    <Image />
  );
};

const styles = StyleSheet.create({

});

export default CustomImage;
